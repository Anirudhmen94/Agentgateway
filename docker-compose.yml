services:
  gateway:
    build: .
    ports:
      - "8000:8000"
    environment:
      GATEWAY_HOST: 0.0.0.0  # container override; process default without this env is 127.0.0.1
      GATEWAY_PORT: 8000
      GATEWAY_TOKEN: ${GATEWAY_TOKEN:-change-me-before-demo}
      XAI_API_KEY: ${XAI_API_KEY:-}
